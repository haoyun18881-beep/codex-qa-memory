interface:
  display_name: "Codex QA Diary Recall"
  short_description: "QA 日记原话、日期、证据和 session 取证"
  default_prompt: "Use $codex-qa-diary-recall only for exact QA diary evidence, dates, quotes, chat records, session/thread IDs, or raw-record fallback after $codex-qa-memory is insufficient."
