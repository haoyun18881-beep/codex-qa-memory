interface:
  display_name: "Codex QA Memory（已退役）"
  short_description: "仅供显式审计冻结的旧 QA memory 档案"
  default_prompt: "Use $codex-qa-memory only to audit a frozen retired QA memory archive without writing or reactivating nodes."
policy:
  allow_implicit_invocation: false
